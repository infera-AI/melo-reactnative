# 音频文件上传功能使用指南

## 概述

在HeadphoneTranslationScreen页面中新增了音频文件上传功能，允许用户选择本地音频文件并通过WebSocket发送进行实时翻译。

## 功能特性

### 🎵 支持的音频格式
- **MP3** - 最常用的音频格式
- **WAV** - 无损音频格式
- **M4A** - Apple设备常用格式
- **AAC** - 高质量压缩格式
- **OGG** - 开源音频格式

### 📏 文件大小限制
- 最大文件大小：**100MB**
- 建议文件大小：**1-10MB**（最佳性能）

### 🔗 WebSocket集成
- 自动检查连接状态
- 连接断开时自动重连
- 分块发送大文件
- 实时进度反馈

## 使用方法

### 1. 选择音频文件
1. 在HeadphoneTranslationScreen页面底部找到**📁**按钮
2. 点击按钮打开文件选择器
3. 选择支持的音频文件
4. 系统会显示文件信息确认

### 2. 发送音频文件
1. 选择文件后，按钮图标变为**📤**
2. 点击发送按钮开始上传
3. 按钮变为**⏳**表示正在发送
4. 发送完成后会收到翻译结果

### 3. 管理文件
- **移除文件**：点击文件信息区域的**✕**按钮
- **重新选择**：移除当前文件后可以重新选择

## 界面说明

### 按钮状态
| 状态 | 图标 | 颜色 | 说明 |
|------|------|------|------|
| 初始 | 📁 | 紫色 | 选择文件 |
| 已选择 | 📤 | 深紫色 | 发送文件 |
| 发送中 | ⏳ | 橙色 | 正在上传 |

### 文件信息显示
```
📁 文件名.mp3
    1.5 MB • MP3
    正在发送...
```

## 技术实现

### 文件选择
```typescript
const handleSelectAudioFile = async () => {
  const result = await DocumentPicker.pick({
    type: [DocumentPicker.types.audio],
    copyTo: 'cachesDirectory',
  });
  // 验证文件格式和大小
  // 设置文件信息
};
```

### 文件发送
```typescript
const handleSendAudioFile = async () => {
  // 检查WebSocket连接
  // 读取文件数据
  // 分块发送
  // 发送结束标记
};
```

### 分块发送策略
- **块大小**：4KB
- **发送间隔**：100ms
- **进度反馈**：实时显示发送进度

## 错误处理

### 常见错误及解决方案

| 错误类型 | 原因 | 解决方案 |
|----------|------|----------|
| 格式不支持 | 文件格式不在支持列表中 | 选择支持的音频格式 |
| 文件过大 | 超过100MB限制 | 选择较小的文件或压缩音频 |
| 连接失败 | WebSocket连接断开 | 检查网络连接，系统会自动重连 |
| 发送失败 | 网络问题或服务器错误 | 重试发送或检查网络状态 |

### 错误提示
- 用户友好的错误提示
- 具体的错误原因说明
- 建议的解决方案

## 性能优化

### 文件处理
- 使用缓存目录存储临时文件
- 分块读取避免内存溢出
- 及时清理临时文件

### 网络传输
- 分块发送减少延迟
- 连接状态监控
- 自动重连机制

### UI响应
- 异步处理避免界面卡顿
- 实时状态更新
- 进度指示器

## 测试验证

### 功能测试
运行测试脚本验证功能：
```bash
node test-audio-upload-feature.js
```

### 测试覆盖
- ✅ 文件格式验证
- ✅ 文件大小验证
- ✅ WebSocket连接检查
- ✅ 音频数据分块发送
- ✅ 错误处理
- ✅ UI状态管理

## 依赖项

### 必需依赖
```json
{
  "@react-native-documents/picker": "^10.1.5",
  "react-native-fs": "^2.20.0"
}
```

### 检查依赖
```bash
npm list @react-native-documents/picker react-native-fs
```

## 注意事项

### 开发环境
- 确保依赖项已正确安装
- 检查文件权限设置
- 测试不同文件格式

### 生产环境
- 文件大小限制配置
- 网络超时设置
- 错误日志记录

### 用户体验
- 文件选择过程流畅
- 发送进度清晰可见
- 错误提示友好易懂

## 更新日志

### v1.0.0 (当前版本)
- ✅ 支持多种音频格式
- ✅ 文件大小限制
- ✅ WebSocket集成
- ✅ 分块发送
- ✅ 错误处理
- ✅ UI状态管理

## 技术支持

### 常见问题
1. **文件选择器不显示**
   - 检查DocumentPicker权限
   - 确认依赖项安装正确

2. **发送失败**
   - 检查网络连接
   - 验证WebSocket状态
   - 查看控制台错误信息

3. **文件格式不支持**
   - 确认文件扩展名正确
   - 检查支持格式列表

### 调试信息
启用详细日志查看：
```typescript
console.log('📁 [HeadphoneTranslationScreen] 开始选择音频文件...');
console.log('📤 [HeadphoneTranslationScreen] 开始发送音频文件...');
```

## 总结

音频文件上传功能已成功集成到HeadphoneTranslationScreen，提供了完整的文件选择、验证、发送和错误处理流程。用户可以通过简单的操作上传音频文件进行实时翻译，享受便捷的语音翻译体验。 